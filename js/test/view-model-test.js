//@ sourceMappingURL=view-model-test.map
// Generated by CoffeeScript 1.6.1
(function() {

  define(['main', '../node_modules/chai/chai.js', 'view-model'], function(Main, Chai, ViewModel) {
    var assert, expect;
    assert = Chai.assert;
    expect = Chai.expect;
    return describe('ViewModel', function() {
      describe('#constructor', function() {});
      describe('#find(letters)', function() {});
      return describe('#filter(words, letters)', function() {
        return it('should filter array by required letters', function() {
          var letters, result, words;
          words = ['abc', 'bcd', 'cde', 'def'];
          letters = 'bc';
          result = ViewModel.filter(words, letters);
          assert.equal(result.length, 2, result);
          assert.include(result, 'abc');
          return assert.include(result, 'bcd');
        });
      });
    });
  });

}).call(this);
