{
  "name": "letterpress-cheat",
  "version": "2.0.0",
  "description": "Find every word you can spell from a set of letters — a helper for the Letterpress word game.",
  "type": "module",
  "license": "MIT",
  "homepage": "https://letterpress.klimenko.dk",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^7.0.0",
    "vitest": "^4.1.8"
  }
}
